#! NX/KF 4.0

DefClass: swd_constants (ug_base_part);

#+
returns the default font height. wrapped as function so it can be used in other functions.
Damn KF quirks.
#-
defun: swd_defaultFontHeight()
@{
 100.0;
}Number;
